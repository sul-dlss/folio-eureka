extraDeploy:
  - |
    apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    metadata:
      name: edge-ncip-backend
      namespace: folio-test
    spec:
      rules:
      - backendRefs:
        - name: edge-ncip
          port: 80
        matches:
          - path:
              type: PathPrefix
              value: /
      hostnames:
      - folio-ncip-test.stanford.edu
      parentRefs:
      - group: gateway.networking.k8s.io
        kind: ListenerSet
        name: folio-test-listeners
        namespace: folio-test
        sectionName: folio-ncip-test
    ---
    apiVersion: gateway.envoyproxy.io/v1alpha1
    kind: SecurityPolicy
    metadata:
      name: edge-ncip-cors
      namespace: folio-test
    spec:
        targetRefs:
          - kind: HTTPRoute
            group: gateway.networking.k8s.io
            name: edge-ncip-backend
        cors:
          allowOrigins:
          - http://kong:8000
          allowMethods:
          - GET
          - OPTIONS
          - POST
          - PUT
