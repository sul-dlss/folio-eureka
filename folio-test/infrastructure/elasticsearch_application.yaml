apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: elasticsearch
  namespace: folio-test
spec:
  project: folio-test
  destination:
    server: https://kubernetes.default.svc
    namespace: folio-test
  sources:
    - repoURL: oci://registry-1.docker.io/bitnamicharts/elasticsearch
      chart: elasticsearch
      targetRevision: 19.6.0
      helm:
        valueFiles:
        - $values/folio-test/infrastructure/elasticsearch.yaml
    - repoURL: https://github.com/sul-dlss/folio-eureka
      targetRevision: folio-test-infrastructure
      ref: values