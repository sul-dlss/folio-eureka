apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mail
  namespace: folio-test
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-test
    server: https://kubernetes.default.svc
  sources:
    - chart: mail
      repoURL: https://bokysan.github.io/docker-postfix
      targetRevision: 5.1.0
      helm:
        valueFiles:
          - $values/folio-test/infrastructure/postfix.yaml
    - repoURL: https://github.com/sul-dlss/folio-eureka
      targetRevision: main
      ref: values
  project: folio-test
