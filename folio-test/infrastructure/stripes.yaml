image:
  pullPolicy: Always
  repository: ghcr.io/sul-dlss/folio-platform-lsp
  tag: R1-2025-csp-7-eureka-test
replicaCount: 2

startupProbeEnabled : true
livenessProbeEnabled : true
readinessProbeEnabled : true

extraDeploy:
  - |
    apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    metadata:
      name: platform-complete-backend
      namespace: folio-test
      labels:
        access: cors-ip-allowlist
    spec:
      rules:
      - backendRefs:
        - name: platform-complete
          port: 80
        matches:
          - path:
              type: PathPrefix
              value: /
      hostnames:
      - folio-test.stanford.edu
      parentRefs:
      - name: default
        namespace: envoy-gateway-system
