apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: folio-dev
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  sources:
    - chart: keycloak
      repoURL: oci://registry-1.docker.io/bitnamicharts/keycloak
      targetRevision: 25.2.0
      helm:
        valueFiles:
          - $values/folio-dev/keycloak.yaml
        skipCrds: true
    - repoURL: https://github.com/sul-dlss/folio-eureka
      targetRevision: kong-gateway
      ref: values
  project: folio-dev
