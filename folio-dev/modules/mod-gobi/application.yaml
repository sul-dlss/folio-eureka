apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-gobi
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-gobi
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-gobi/overrides.yaml
      - $values/folio-dev/modules/mod-gobi/resources.yaml
      - $values/common/sidecar.yaml
      - $values/common/probes.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 2.7.49
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
