apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-calendar
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-calendar
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-calendar/overrides.yaml
      - $values/folio-dev/modules/mod-calendar/resources.yaml
      - $values/common/sidecar.yaml
      - $values/common/probes.yaml
      - $values/folio-dev/modules/mod-calendar/java_opts.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 2.5.48
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
