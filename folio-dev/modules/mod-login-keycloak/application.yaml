apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-login-keycloak
  namespace: folio-dev
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-login-keycloak
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-login-keycloak/overrides.yaml
      - $values/folio-dev/common/resources.yaml
      - $values/folio-dev/common/sidecar.yaml
      - $values/folio-dev/common/probes.yaml
      - $values/folio-dev/common/java_opts.yaml
      - $values/folio-dev/modules/mod-login-keycloak/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 0.0.56
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
