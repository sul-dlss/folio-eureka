apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-licenses
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-licenses
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-licenses/overrides.yaml
      - $values/folio-dev/modules/mod-licenses/resources.yaml
      - $values/common/sidecar.yaml
      - $values/folio-dev/modules/mod-licenses/probes.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 5.0.50
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
