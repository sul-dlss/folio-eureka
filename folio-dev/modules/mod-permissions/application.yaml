apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-permissions
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-permissions
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-permissions/overrides.yaml
      - $values/common/resources.yaml
      - $values/common/sidecar.yaml
      - $values/common/probes.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 6.4.48
  - ref: values
    repoURL: git@github.com:sul-dlss/folio-eureka.git
    targetRevision: argo-cd
