apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: edge-connexion
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: edge-connexion
    helm:
      valueFiles:
      - $values/folio-dev/modules/edge-connexion/overrides.yaml
      - $values/folio-dev/modules/edge-connexion/resources.yaml
      - $values/common/sidecar.yaml
      - $values/common/probes.yaml
      - $values/folio-dev/modules/edge-connexion/service.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 1.1.49
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
