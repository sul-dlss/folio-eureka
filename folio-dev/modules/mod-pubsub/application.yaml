apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-pubsub
  namespace: folio-dev
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-pubsub
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-pubsub/overrides.yaml
      - $values/folio-dev/modules/mod-pubsub/resources.yaml
      - $values/folio-dev/common/sidecar.yaml
      - $values/folio-dev/common/probes.yaml
      - $values/folio-dev/common/java_opts.yaml
      - $values/folio-dev/modules/mod-pubsub/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 3.0.46
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
