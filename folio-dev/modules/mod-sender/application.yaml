apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-sender
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-sender
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-sender/overrides.yaml
      - $values/folio-dev/modules/mod-sender/resources.yaml
      - $values/common/sidecar.yaml
      - $values/folio-dev/modules/mod-sender/probes.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 1.11.48
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
