apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-data-export
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-data-export
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-data-export/overrides.yaml
      - $values/folio-dev/modules/mod-data-export/resources.yaml
      - $values/common/sidecar.yaml
      - $values/common/probes.yaml
      - $values/folio-dev/modules/mod-data-export/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 4.9.43
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
