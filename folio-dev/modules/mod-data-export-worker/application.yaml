apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-data-export-worker
  namespace: folio-dev
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-dev
    server: https://kubernetes.default.svc
  project: folio-dev
  sources:
  - chart: mod-data-export-worker
    helm:
      valueFiles:
      - $values/folio-dev/modules/mod-data-export-worker/overrides.yaml
      - $values/folio-dev/modules/mod-data-export-worker/resources.yaml
      - $values/common/sidecar.yaml
      - $values/common/probes.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 3.2.41
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: argo-cd
