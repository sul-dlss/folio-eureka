apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mgr-tenants
  namespace: folio-prod
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-prod
    server: https://kubernetes.default.svc
  project: folio-prod
  sources:
  - chart: mgr-tenants
    helm:
      valueFiles:
      - $values/folio-prod/modules/mgr-tenants/mgr-tenants.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 0.0.16
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
