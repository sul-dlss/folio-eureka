apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-permissions
  namespace: folio-prod
spec:
  destination:
    namespace: folio-prod
    server: https://kubernetes.default.svc
  project: folio-prod
  sources:
  - chart: mod-permissions
    helm:
      valueFiles:
      - $values/folio-prod/modules/mod-permissions/overrides.yaml
      - $values/folio-prod/modules/mod-permissions/resources.yaml
      - $values/folio-prod/common/sidecar.yaml
      - $values/folio-prod/common/probes.yaml
      - $values/folio-prod/common/java_opts.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 6.4.48
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
