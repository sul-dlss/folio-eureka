apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-orders
  namespace: folio-prod
spec:
  destination:
    namespace: folio-prod
    server: https://kubernetes.default.svc
  project: folio-prod
  sources:
  - chart: mod-orders
    helm:
      valueFiles:
      - $values/folio-prod/modules/mod-orders/overrides.yaml
      - $values/folio-prod/modules/mod-orders/resources.yaml
      - $values/folio-prod/common/sidecar.yaml
      - $values/folio-prod/common/probes.yaml
      - $values/folio-prod/modules/mod-orders/replicas.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 12.7.49
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
