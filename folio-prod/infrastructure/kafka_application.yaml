apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka-folio-prod
  namespace: folio-prod
  labels:
    type: infrastructure
spec:
  project: folio-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: folio-prod
  sources:
    - repoURL: https://charts.bitnami.com/bitnami
      chart: kafka
      targetRevision: 31.1.0
      helm:
        valueFiles:
        - $values/folio-prod/infrastructure/kafka.yaml
    - repoURL: https://github.com/sul-dlss/folio-eureka
      targetRevision: main
      ref: values