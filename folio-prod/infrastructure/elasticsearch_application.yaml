apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: elasticsearch
  namespace: folio-prod
  labels:
    type: infrastructure
spec:
  project: folio-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: folio-prod
  sources:
    - repoURL: oci://registry-1.docker.io/bitnamicharts/elasticsearch
      chart: elasticsearch
      targetRevision: 19.6.0
      helm:
        valueFiles:
        - $values/folio-prod/infrastructure/elasticsearch.yaml
    - repoURL: https://github.com/sul-dlss/folio-eureka
      targetRevision: main
      ref: values