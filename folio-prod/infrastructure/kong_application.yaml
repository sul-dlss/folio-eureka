apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kong
  namespace: folio-prod
spec:
  destination:
    namespace: folio-prod
    server: https://kubernetes.default.svc
  sources:
    - chart: kong
      repoURL: oci://registry-1.docker.io/bitnamicharts/kong
      targetRevision: 12.0.11
      helm:
        valueFiles:
          - $values/folio-prod/infrastructure/kong.yaml
        skipCrds: true
    - repoURL: https://github.com/sul-dlss/folio-eureka
      targetRevision: main
      ref: values
  project: folio-prod
