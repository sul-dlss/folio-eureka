apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-data-export
  namespace: folio-stage
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-data-export
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-data-export/overrides.yaml
      - $values/folio-stage/modules/mod-data-export/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/modules/mod-data-export/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 4.9.43
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
