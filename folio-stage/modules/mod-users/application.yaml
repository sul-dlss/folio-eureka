apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-users
  namespace: folio-stage
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-users
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-users/overrides.yaml
      - $values/folio-stage/modules/mod-users/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/modules/mod-users/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2/
    targetRevision: 19.3.41
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
