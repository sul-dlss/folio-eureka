apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-source-record-manager
  namespace: folio-stage
operation:
  sync:
    syncStrategy:
      hook: {}
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-source-record-manager
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-source-record-manager/overrides.yaml
      - $values/folio-stage/modules/mod-source-record-manager/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 3.8.43
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
