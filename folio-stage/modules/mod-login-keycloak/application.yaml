apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    application: module
  name: mod-login-keycloak
  namespace: folio-stage
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-login-keycloak
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-login-keycloak/overrides.yaml
      - $values/folio-stage/common/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/common/java_opts.yaml
      - $values/folio-stage/modules/mod-login-keycloak/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 0.0.56
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
