apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-event-config
  namespace: folio-stage
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-event-config
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-event-config/overrides.yaml
      - $values/folio-stage/modules/mod-event-config/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/common/java_opts.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 2.6.48
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
