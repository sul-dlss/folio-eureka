apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mod-password-validator
  namespace: folio-stage
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-password-validator
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-password-validator/overrides.yaml
      - $values/folio-stage/modules/mod-password-validator/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/common/java_opts.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 3.1.48
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
