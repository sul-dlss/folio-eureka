apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    application: module
  name: mod-inventory-storage
  namespace: folio-stage
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-inventory-storage
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-inventory-storage/overrides.yaml
      - $values/folio-stage/modules/mod-inventory-storage/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/common/java_opts.yaml
      - $values/folio-stage/modules/mod-inventory-storage/extra_env.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 27.1.47
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
