apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    application: app-platform-minimal-2.0.52
    type: module
  name: mod-configuration
  namespace: folio-stage
spec:
  destination:
    namespace: folio-stage
    server: https://kubernetes.default.svc
  project: folio-stage
  sources:
  - chart: mod-configuration
    helm:
      valueFiles:
      - $values/folio-stage/modules/mod-configuration/overrides.yaml
      - $values/folio-stage/modules/mod-configuration/resources.yaml
      - $values/folio-stage/common/sidecar.yaml
      - $values/folio-stage/common/probes.yaml
      - $values/folio-stage/common/java_opts.yaml
    repoURL: https://sul-dlss.github.io/folio-helm-v2
    targetRevision: 5.9.55
  - ref: values
    repoURL: https://github.com/sul-dlss/folio-eureka
    targetRevision: main
